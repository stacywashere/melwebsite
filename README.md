# Mei profile

A static personal profile page with live Discord presence and activity data.

## GitHub Pages

Push the repository to GitHub on the `main` branch, then open **Settings → Pages** and select **GitHub Actions** as the source. The included workflow builds and deploys the site automatically to the configured custom domain.

## Local development

```bash
npm install
npm run dev
```
